# Exemple de fichier .env pour BreakIn Direct
# Copier ce fichier vers .env et remplir avec vos vraies valeurs

# === Configuration MongoDB ===
# Pour développement local
MONGO_URI=mongodb://localhost:27017

# Pour MongoDB Atlas (Production/Hackathon)
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/

# Nom de la base de données
DB_NAME=breakin_hackathon

# === Configuration GPT-5 ===
# Clé API OpenAI (obligatoire)
GPT_API_KEY=your_openai_api_key_here

# === Configuration FastAPI ===
# Environnement (development, production)
ENVIRONMENT=development

# Secret pour la signature des tokens
SECRET_KEY=your_super_secret_key_here

# === Configuration Logging ===
# Niveau de log (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# === Configuration CORS ===
# URLs autorisées pour CORS (séparées par des virgules)
ALLOWED_ORIGINS=http://localhost:3000,https://breakin-direct.vercel.app

# === Configuration Email (optionnel) ===
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASSWORD=your_app_password
